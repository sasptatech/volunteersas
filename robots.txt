# VolunteerSAS — deliberately not indexed by search engines.
#
# The whole app sits behind a sign-in gate, so there is no public content to
# index — and we don't want the login page surfaced in search or the URL
# advertised. This is a private SAS-community tool reached via a shared invite
# link, not a public website. Keep this as-is unless the PTA specifically wants
# the site discoverable in Google (in which case, revisit — but there's little
# point while everything requires login).
User-agent: *
Disallow: /
